# HouseHunt Kenya - Backend (scaffold)

This folder contains a minimal Express + Mongoose backend scaffold.

To run:

```bash
cd server
npm install
cp .env.example .env
# edit .env if needed, then:
npm run dev
```

Endpoints are exposed under `/api/*`.
